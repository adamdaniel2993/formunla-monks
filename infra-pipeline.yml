trigger:
  branches:
    include:
    - main
  paths:
    include:
    - terraform/*
    - infra-pipeline.yml

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: infra-dev-vg

stages:
- stage: Terraform
  jobs:
  - job: TerraformDeployment
    displayName: 'Terraform Deployment'
    steps:
    - checkout: self
    - task: UseTerraform@0
      inputs:
        version: $(terraformVersion)

    - script: |
        terraform workspace list  || terraform workspace select $(environment)
      displayName: 'Select Terraform Workspace'
      workingDirectory: $(workingDirectory)

    - script: |
        terraform init
      displayName: 'Terraform Init'
      workingDirectory: $(workingDirectory)
      env:
        ARM_SUBSCRIPTION_ID: $(serviceConnectionName)
        # Add other required environment variables like ARM_CLIENT_ID, etc.

    - script: |
        terraform plan -out=tfplan
      displayName: 'Terraform Plan'
      workingDirectory: $(workingDirectory)

    - script: |
        terraform apply -auto-approve tfplan
      displayName: 'Terraform Apply'
      workingDirectory: $(workingDirectory)

    - task: PublishBuildArtifacts@1
      inputs:
        PathtoPublish: '$(workingDirectory)/tfplan'
        ArtifactName: 'tfplan'
        publishLocation: 'container'
